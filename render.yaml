services:
  - type: web
    name: travel-planner-app
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3019
      - key: MONGODB_URI
        sync: false
